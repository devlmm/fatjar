<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!--
  ====================================================================================
  OaApprovalMapper.xml : OA 审批手写 SQL
  ------------------------------------------------------------------------------------
  namespace 与 Java 接口全限定名一致，由 MyBatis 自动绑定。
  基础 CRUD（save/getById/update/remove 等）由 BaseMapper 提供，此处暂无自定义方法。
  注意：手写 SQL 不会自动应用 MyBatis-Plus 全局逻辑删除，需手动添加 deleted = 0 条件。
  ====================================================================================
-->
<mapper namespace="com.workspace.fatjar.oa.mapper.OaApprovalMapper">
</mapper>
